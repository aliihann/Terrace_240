@model Terrace_240.DAL.Model.Movie
@* @{
    Layout = "_Layout";
} *@
@* <h2>Добавить фильм</h2>
<form method="post" enctype="multipart/form-data">
    <div><label>Название</label><input name="Title" value="@Model.Title" required /></div>
    <div><label>Год</label><input name="Year" type="number" value="@Model.Year" /></div>
    <div><label>Жанр</label><input name="Genre" value="@Model.Genre" /></div>
    <div><label>Рейтинг</label><input name="Rating" type="number" step="0.1" value="@Model.Rating" /></div>
    <div><label>Постер (файл)</label><input name="poster" type="file" accept="image/*" /></div>
    <div><label>Описание</label><textarea name="Description">@Model.Description</textarea></div>
    <div><label>Время сеансов (в строку, через запятую)</label><input name="ScheduleTimes" value="@Model.ScheduleTimes" /></div>
    <div><label>Цена</label><input name="Price" type="number" value="@Model.Price" /></div>
    <button type="submit">Сохранить</button>
</form> *@

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Добавить фильм - Tarrace240</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/movie-form.css">
</head>
<body>
    <header class="header">
        <div class="header-content">
            <div class="logo">
                <img src="~/uploads/Image_240.jpg" alt="Tarrace240" class="logo-image">
                <span class="logo-text">Tarrace240</span>
            </div>
            <nav class="nav">
                <a asp-action="Index" asp-controller="Admin" class="nav-link">
                    <i class="fas fa-arrow-left"></i>
                    <span>Назад</span>
                </a>
            </nav>
        </div>
    </header>

    <main class="main">
        <div class="container">
            <div class="page-header">
                <h1>Добавить новый фильм</h1>
                <p>Заполните все поля для добавления фильма в афишу</p>
            </div>

            <div class="form-container">
                <form method="post" enctype="multipart/form-data" class="movie-form">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="movieTitle">
                                <i class="fas fa-film"></i>
                                Название фильма *
                            </label>
                            <input name="Title" value="@Model.Title" type="text" id="movieTitle"  required placeholder="Введите название фильма">
                        </div>

                        <div class="form-group">
                            <label for="movieYear">
                                <i class="fas fa-calendar"></i>
                                Год выпуска *
                            </label>
                            <input name="Year" type="number" value="@Model.Year"  id="movieYear" required min="1900" max="2030" placeholder="2024">
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label for="movieGenre">
                                <i class="fas fa-tags"></i>
                                Жанр *
                            </label>
                            <select name="Genre" value="@Model.Genre" id="movieGenre" required>
                                <option value="">Выберите жанр</option>
                                <option value="Боевик">Боевик</option>
                                <option value="Комедия">Комедия</option>
                                <option value="Драма">Драма</option>
                                <option value="Фантастика">Фантастика</option>
                                <option value="Триллер">Триллер</option>
                                <option value="Ужасы">Ужасы</option>
                                <option value="Романтика">Романтика</option>
                                <option value="Приключения">Приключения</option>
                                <option value="Анимация">Анимация</option>
                                <option value="Документальный">Документальный</option>
                                <option value="Биография">Биография</option>
                                <option value="Криминал">Криминал</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label for="movieRating">
                                <i class="fas fa-star"></i>
                                Рейтинг *
                            </label>
                            <input name="Rating" type="number"  value="@Model.Rating"  id="movieRating"  required min="0" max="10" step="0.1" placeholder="8.5">
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="moviePoster">
                            <i class="fas fa-image"></i>
                            Постер фильма(файл)
                        </label>
                        <div class="file-input-container">
                            <input name="poster" type="file" id="moviePoster" class="file-input" accept="image/*" />
                            <label for="moviePoster" class="file-input-label">
                                <i class="fas fa-cloud-upload-alt"></i>
                                <span>Выберите файл или перетащите сюда</span>
                            </label>
                            <div class="file-preview" id="filePreview"></div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="movieDescription">
                            <i class="fas fa-align-left"></i>
                            Описание *
                        </label>
                        <textarea id="movieDescription" name="Description" required rows="4" placeholder="Введите описание фильма">@Model.Description</textarea>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label for="moviePrice">
                                <i class="fas fa-ruble-sign"></i>
                                Цена билета *
                            </label>
                            <input name="Price" type="number" value="@Model.Price"  id="moviePrice"  required min="0" placeholder="300">
                        </div>

                        <div class="form-group">
                            <label for="movieSchedule">
                                <i class="fas fa-clock"></i>
                                Расписание сеансов *
                            </label>
                            <input name="ScheduleTimes" value="@Model.ScheduleTimes" type="text" id="movieSchedule"  required placeholder="19:00, 21:30, 23:00">
                            <small>Введите время сеансов через запятую (формат: ЧЧ:ММ)</small>
                        </div>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-plus"></i>
                            Добавить фильм
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <div id="toast" class="toast"></div>

</body>
</html>
